# OpenAI for niche validation + relevance filtering
OPENAI_API_KEY=sk-...

# Telegram bot (NEW dedicated ASO bot — create via @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_CHAT_ID=-1001234567890

# Seed keywords endpoint (filled in once user provides it)
SEED_KEYWORDS_ENDPOINT=https://example.com/api/seeds

# upup auth (path to playwright storage state with logged-in session)
UPUP_AUTH_STATE=./cache/auth_state.json
